- name: mediawiki123-apb provision
  hosts: localhost
  gather_facts: false
  connection: local
  roles:
  - role: ansible.kubernetes-modules
    install_python_requirements: no
  - mediawiki
  vars:
    action: provision
    cluster: kubernetes
